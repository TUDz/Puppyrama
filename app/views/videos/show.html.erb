<h1>Puppy  <%= @video.title %></h1>
<%= link_to "Edit", edit_video_path(@video), class: "btn btn-primary"%>
<%= button_to "Delete", delete_video_path(@video), method: :delete, class: "btn btn-danger" %>